name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    paths-ignore:
      - 'api/docs/**'
      - 'build/charts/**'
      - '**/*.md'
      - '**/*.txt'
      - '**/.gitignore'

env:
  GO_VERSION: '1.21'

jobs:
  build:
    uses: ./ci/build-job.yml

  bench:
    uses: ./ci/bench-job.yml

  sharding_test:
    uses: ./ci/sharding-test-job.yml
